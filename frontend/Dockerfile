# Use a lightweight web server to serve static files
FROM nginx:alpine

# Copy the HTML, JS, and CSS files to the web server's root directory
COPY . /usr/share/nginx/html

# Expose the default HTTP port
EXPOSE 80
